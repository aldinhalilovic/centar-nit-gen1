export default function sumNumbers(x, y) {
  return x + y;
}

export function sumNumbers(x, y) {
  return x + y;
}

export function sumNumbers(x, y) {
  return x + y;
}

export function sumNumbers(x, y) {
  return x + y;
}

export function sumNumbers(x, y) {
  return x + y;
}

export function sumNumbers(x, y) {
  return x + y;
}

export function sumNumbers(x, y) {
  return x + y;
}

export function sumNumbers(x, y) {
  return x + y;
}
